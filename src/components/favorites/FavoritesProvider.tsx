'use client'
// FavoritesProvider mantido por compatibilidade — inicialização real é feita pelo CartProvider
export function FavoritesProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
